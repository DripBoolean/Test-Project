{
    "tasks": [
        {
            "label": "Cmake build",
            "type": "shell",
            "command": "cd Build/CMake && cmake -B./ -S../../src && make",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ],
    "version": "2.0.0"
}